name: Test, Release and Publish

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  test:
    name: Test and Lint Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.12', '3.13', '3.14']
      fail-fast: false

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}

      - name: Cache pip packages
        uses: actions/cache@v6
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ matrix.python-version }}-${{ hashFiles('**/pyproject.toml') }}
          restore-keys: |
            ${{ runner.os }}-pip-${{ matrix.python-version }}-
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run ruff (linting)
        run: ruff check src/ tests/ --output-format=github

      - name: Run black (code formatting check)
        run: black --check --diff src/ tests/

      - name: Run mypy (type checking)
        run: mypy src/check_netscaler_gateway/

      - name: Run tests
        run: pytest tests/ -vv --tb=short

      - name: Test CLI
        run: |
          check_netscaler_gateway --help
          check_netscaler_gateway --version

  build:
    name: Build Package
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.14'

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build twine

      - name: Build package
        run: python -m build

      - name: Check package
        run: twine check dist/*

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: dist
          path: dist/

  build-binaries:
    name: Build Binaries (${{ matrix.os }})
    needs: test
    runs-on: ${{ matrix.os }}
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        include:
          - os: ubuntu-latest
            artifact_name: check_netscaler_gateway-linux
            asset_name: check_netscaler_gateway-linux
          - os: windows-latest
            artifact_name: check_netscaler_gateway.exe
            asset_name: check_netscaler_gateway-windows.exe
          - os: macos-latest
            artifact_name: check_netscaler_gateway-macos
            asset_name: check_netscaler_gateway-macos

    steps:
    - name: Checkout code
      uses: actions/checkout@v7

    - name: Set up Python 3.14
      uses: actions/setup-python@v7
      with:
        python-version: '3.14'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e .
        pip install pyinstaller

    - name: Build with PyInstaller
      run: |
        pyinstaller check_netscaler_gateway.spec

    - name: Smoke test (Linux/macOS)
      if: runner.os != 'Windows'
      run: |
        chmod +x dist/check_netscaler_gateway
        ./dist/check_netscaler_gateway --version
        ./dist/check_netscaler_gateway --help

    - name: Smoke test (Windows)
      if: runner.os == 'Windows'
      run: |
        dist\check_netscaler_gateway.exe --version
        dist\check_netscaler_gateway.exe --help

    - name: Rename binary (Linux)
      if: runner.os == 'Linux'
      run: mv dist/check_netscaler_gateway dist/${{ matrix.artifact_name }}

    - name: Rename binary (macOS)
      if: runner.os == 'macOS'
      run: mv dist/check_netscaler_gateway dist/${{ matrix.artifact_name }}

    - name: Generate SHA256 checksum (Linux/macOS)
      if: runner.os != 'Windows'
      run: |
        cd dist
        sha256sum ${{ matrix.artifact_name }} > ${{ matrix.artifact_name }}.sha256sum

    - name: Generate SHA256 checksum (Windows)
      if: runner.os == 'Windows'
      run: |
        cd dist
        certutil -hashfile ${{ matrix.artifact_name }} SHA256 > ${{ matrix.artifact_name }}.sha256sum

    - name: Upload binary artifact
      uses: actions/upload-artifact@v7
      with:
        name: ${{ matrix.artifact_name }}
        path: |
          dist/${{ matrix.artifact_name }}
          dist/${{ matrix.artifact_name }}.sha256sum
        if-no-files-found: error

  release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: [test, build, build-binaries]
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    outputs:
      released: ${{ steps.semantic.outputs.released }}
      version: ${{ steps.semantic.outputs.version }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.14'

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build twine

      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts/

      - name: Display artifact structure
        run: ls -R artifacts/

      - name: Python Semantic Release
        id: semantic
        uses: python-semantic-release/python-semantic-release@v10.6.2
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          root_options: "-vv"

      - name: Upload binaries to release
        if: steps.semantic.outputs.released == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION=${{ steps.semantic.outputs.version }}
          gh release upload v${VERSION} \
            artifacts/check_netscaler_gateway-linux/check_netscaler_gateway-linux \
            artifacts/check_netscaler_gateway-linux/check_netscaler_gateway-linux.sha256sum \
            artifacts/check_netscaler_gateway.exe/check_netscaler_gateway.exe \
            artifacts/check_netscaler_gateway.exe/check_netscaler_gateway.exe.sha256sum \
            artifacts/check_netscaler_gateway-macos/check_netscaler_gateway-macos \
            artifacts/check_netscaler_gateway-macos/check_netscaler_gateway-macos.sha256sum \
            --clobber

  publish-testpypi:
    name: Publish to TestPyPI
    runs-on: ubuntu-latest
    needs: release
    if: needs.release.outputs.released == 'true'
    continue-on-error: true
    environment:
      name: testpypi
      url: https://test.pypi.org/project/check_netscaler_gateway/

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          ref: v${{ needs.release.outputs.version }}

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.14'

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build twine

      - name: Build package
        run: python -m build

      - name: Publish to TestPyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/
          skip-existing: true

  publish-pypi:
    name: Publish to PyPI
    runs-on: ubuntu-latest
    needs: release
    if: needs.release.outputs.released == 'true' && always()
    environment:
      name: pypi
      url: https://pypi.org/project/check_netscaler_gateway/

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          ref: v${{ needs.release.outputs.version }}

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.14'

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build twine

      - name: Build package
        run: python -m build

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
